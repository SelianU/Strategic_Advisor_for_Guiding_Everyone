"""ai_agents/asteroids/coach_config.py — Asteroids 코칭 설정"""
from __future__ import annotations
from typing import Any

_ACTION_KO: dict[str, str] = {
    "NOOP":          "대기",
    "FIRE":          "발사",
    "UP":            "추진",
    "RIGHT":         "오른쪽 회전",
    "LEFT":          "왼쪽 회전",
    "DOWN":          "하이퍼스페이스(순간이동)",
    "UPRIGHT":       "오른쪽으로 회전하며 추진",
    "UPLEFT":        "왼쪽으로 회전하며 추진",
    "UPFIRE":        "추진하며 발사",
    "RIGHTFIRE":     "오른쪽으로 회전하며 발사",
    "LEFTFIRE":      "왼쪽으로 회전하며 발사",
    "DOWNFIRE":      "하이퍼스페이스 사용하며 발사",
    "UPRIGHTFIRE":   "오른쪽으로 회전하며 추진+발사",
    "UPLEFTFIRE":    "왼쪽으로 회전하며 추진+발사",
}

reward_label = "격파"


def action_name_ko(name: str) -> str:
    return _ACTION_KO.get(name, name)


SYSTEM_PROMPT = """당신은 Asteroids 플레이어님에게 1:1 코칭을 제공하는 게임 전략 코치입니다.
 
역할:
- 플레이어님이 한 행동과 AI 에이전트가 선택한 행동을 비교하고, 에이전트의 선택이 왜 더 좋은 판단이었는지 설명합니다.
- 그 상황에서 어떻게 행동해야 했는지 플레이어님이 다음 판에 바로 써먹을 수 있는 조언을 줍니다.
- 분석 보고서가 아니라, 옆에서 직접 말해주는 코치처럼 자연스럽게 설명하세요.
 
게임 구조와 핵심 규칙:
- 우주 공간에서 우주선을 조종해 소행성과 UFO를 격파하는 슈팅 게임입니다.
- 소행성이나 UFO에 충돌하거나 UFO의 발사체에 맞으면 목숨을 잃습니다.
- 화면 끝에 도달하면 반대편에서 나타납니다(랩핑 화면 구조).
 
조작 메커니즘 — 관성 물리 (매우 중요):
- RIGHT/LEFT는 우주선의 방향을 회전시킵니다. 회전만으로는 이동 방향이 바뀌지 않습니다.
- UP(추진)을 가하면 현재 바라보는 방향으로 속도가 붙습니다. 추진을 멈춰도 즉시 멈추지 않고 계속 이동합니다.
- 브레이크가 없습니다. 멈추려면 반대 방향으로 몸을 돌린 후 추진해 속도를 상쇄해야 합니다.
- 조작 한 번 한 번이 이후 궤도에 직접적인 영향을 주므로, 불필요한 추진은 최대한 자제해야 합니다.
- 회전하면서 동시에 추진하거나 발사하는 복합 액션(예: 오른쪽으로 회전하며 추진+발사)도 가능합니다.
 
하이퍼스페이스 (DOWN):
- 우주선이 화면에서 순간적으로 사라졌다가 랜덤한 위치에 재등장합니다.
- 긴급 회피 수단이지만, 재등장 위치가 소행성 한가운데일 수 있어 사용 시 위험 부담이 있습니다.
 
발사 메커니즘:
- 발사체는 연속으로 최대 2발까지 화면에 존재할 수 있으며, 이후 짧은 텀이 생깁니다.
- 2발을 빠르게 연속 발사해 큰 소행성을 연속 타격하거나, 발사 후 추진으로 따라붙어 분열된 파편들을 연속 격파하는 고난이도 전략도 존재합니다.
 
소행성 분열과 점수:
- 큰 소행성을 맞히면 2개의 중간 소행성으로 분열되고, 중간 소행성을 맞히면 2개의 작은 소행성으로 분열됩니다.
- 작은 소행성일수록 점수가 높습니다. 큰 소행성을 끝까지 분열시켜 작은 파편으로 격파하는 것이 고득점 전략입니다.
- 큰 소행성 격파 후 분열된 파편이 우주선 쪽으로 튀어올 수 있어, 격파 직후 회피 계획이 함께 있어야 합니다.
 
UFO:
- 주기적으로 등장해 우주선을 향해 발사합니다. 격파 시 보너스 점수이지만 위험 부담도 큽니다.
 
코칭 원칙:
- 플레이어님의 행동과 에이전트 행동을 반드시 대비해서 설명하세요.
- "에이전트가 왜 이 타이밍에 이 행동을 선택했는가"를 우주선의 현재 관성 방향, 주변 소행성의 위치와 크기, 분열 후 파편 예상 경로 기준으로 설명하세요.
- 하이퍼스페이스(DOWN)는 랜덤 이동이므로, 에이전트가 이를 선택한 경우 "다른 회피 수단이 없는 극한 상황"이었음을 맥락으로 설명하세요.
- 마지막은 반드시 "다음에 이런 상황이라면..." 스타일의 실천 조언으로 마무리하세요.
- 행동 이름은 한국어 이름만 사용하세요. NOOP, UP, FIRE, RIGHTFIRE 같은 영어 약어는 절대 쓰지 마세요.
- 영어 표현을 섞지 마세요.
"""


# ── 이후 경로 지침 ────────────────────────────────────────────────────────────

def build_outcome_guidance(summary: dict[str, Any]) -> str:
    h_score = summary.get('human_score_delta', 0)
    a_score = summary.get('agent_score_delta', 0)
    h_done  = summary.get('human_done', False)
    a_done  = summary.get('agent_done', False)
    gap     = summary.get('gap', 0)

    if h_done and not a_done:
        return (f"이 비교 구간에서 플레이어님은 목숨을 잃었지만 에이전트는 생존했습니다. "
                f"생존 여부 자체가 이 순간 두 행동의 결과 차이를 보여줍니다. "
                f"에이전트({a_score:.0f}점)가 플레이어님({h_score:.0f}점)보다 높은 점수도 기록했습니다. "
                f"회피 기동과 발사 타이밍 차이가 어떻게 생존 여부로 이어졌는지 설명하세요.")
    if h_done and a_done:
        if a_score > h_score:
            return (f"이 비교 구간에서 두 경로 모두 목숨을 잃었지만, "
                    f"에이전트({a_score:.0f}점)가 플레이어님({h_score:.0f}점)보다 높은 점수를 기록했습니다. "
                    f"점수 차이와 Q값 차이({gap:.4f})를 함께 근거로 활용해 에이전트의 행동이 왜 더 나은 판단이었는지 설명하세요.")
        return (f"이 비교 구간에서 두 경로 모두 목숨을 잃었습니다. "
                f"점수 차이가 크지 않으므로 점수를 결정적 근거로 쓰지 마세요. "
                f"대신 이 순간의 Q값 차이({gap:.4f})가 의미하는 전략적 판단 차이에 집중하세요.")
    if not h_done and a_done:
        return (f"이 비교 구간에서 에이전트 경로가 목숨을 잃었고 플레이어님은 생존했습니다. "
                f"단기 결과로는 플레이어님의 행동이 생존 면에서 앞섰습니다. "
                f"이 점을 솔직하게 인정하되, Q값은 에이전트 행동({summary.get('agent_q', 0):.4f})을 더 높게 평가한다는 점과 "
                f"Q값이 수천 번의 학습에서 통계적으로 더 유리한 판단을 반영한다는 점을 설명하세요. "
                f"이 특정 구간의 결과가 Q값의 장기 판단과 항상 일치하지 않을 수 있음도 자연스럽게 인정하세요.")
    if a_score > h_score:
        return (f"이후 약 30초 비교 구간에서 에이전트({a_score:.0f}점)가 플레이어님({h_score:.0f}점)보다 높은 점수를 기록했습니다. "
                f"격파 프레임 데이터를 활용해 두 경로가 어떻게 달라졌는지 구체적으로 묘사하세요.")
    return (f"이후 약 30초 비교 구간에서는 플레이어님({h_score:.0f}점)의 점수가 에이전트({a_score:.0f}점)와 비슷하거나 더 높습니다. "
            f"이 구간 결과를 직접 비교 근거로 쓰지 마세요. "
            f"대신 이 순간의 Q값 차이({gap:.4f})가 의미하는 전략적 판단 차이에 집중하세요.")


# ── user 프롬프트 ─────────────────────────────────────────────────────────────

def build_user_prompt(summary: dict[str, Any]) -> str:
    h_score      = summary.get('human_score_delta', 0)
    a_score      = summary.get('agent_score_delta', 0)
    outcome_guid = build_outcome_guidance(summary)

    frames    = summary.get('rgb_frames_b64') or []
    has_image = len(frames) > 0
    image_note = (
        "\n- 첨부 이미지 3장([직전] → [결정순간] → [직후]): 행동 전후 실제 게임 화면입니다. "
        "소행성 크기·위치·밀집도, 우주선 방향·관성, 파편 경로의 변화를 직접 확인해 상황을 묘사하세요."
        if has_image else ""
    )

    return f"""다음은 Asteroids 코칭 사례입니다. 아래 정보를 바탕으로 플레이어님에게 자연스러운 한국어 피드백을 작성해주세요.

상황 정보:
- 스텝: {summary['step']}
- 플레이어님 행동: {action_name_ko(summary['human_action_name'])}
- 에이전트 행동: {action_name_ko(summary['agent_action_name'])}
- 플레이어님 행동의 Q값: {summary.get('human_q', 0):.4f}
- 에이전트 행동의 Q값: {summary.get('agent_q', 0):.4f}
- 가치 차이: {summary.get('gap', 0):.4f}
- 이후 30초 비교 구간 플레이어님 점수: {h_score:.1f}
- 이후 30초 비교 구간 에이전트 점수: {a_score:.1f}
- 플레이어님 쪽 첫 격파 시점: {summary.get('human_first_reward_step', '격파 없음')}
- 에이전트 쪽 첫 격파 시점: {summary.get('agent_first_reward_step', '격파 없음')}
- 플레이어님 쪽 격파 프레임들: {summary.get('human_reward_steps') or '없음'}
- 에이전트 쪽 격파 프레임들: {summary.get('agent_reward_steps') or '없음'}
{image_note}이후 경로 활용 지침: {outcome_guid}

작성 방식:
- 문단 수는 2~3개로 자유롭게 구성하세요. 각 문단은 빈 줄 하나로 구분하세요.
- 문단 제목, 레이블, 번호(예: "분석", "1." 등)는 절대 쓰지 마세요. 바로 본문으로 시작하세요.
- 첫 번째 문단: 플레이어님 행동과 에이전트 행동을 명확히 대비하고, "이후 경로 활용 지침"에 따라 이후 전개를 설명하세요. 첨부 이미지 3장이 있다면 [직전]→[결정순간]→[직후] 흐름에서 보이는 소행성 배치·우주선 관성 변화를 배경으로 자연스럽게 녹이세요.
- 중간 문단(선택): 에이전트의 행동이 왜 더 나은 판단이었는지 설명하세요. 관성 물리로 인해 조작 하나하나가 이후 궤도에 미치는 영향, 소행성 크기와 분열 파편 경로, 연속 발사 타이밍 등을 자연스럽게 활용하세요.
- 마지막 문단: "다음에 이런 상황이라면" 또는 그와 비슷한 뉘앙스로 시작해 플레이어님이 바로 실천할 수 있는 구체적인 조언을 주세요.
- 점수와 격파 프레임은 이 순간 이후 약 30초 비교 구간의 수치입니다. 점수를 언급할 때는 "이 구간에서" 또는 "비교 구간에서"라고 명시하세요.
- Q값 차이가 전략적으로 의미하는 바를 자연스럽게 녹이는 방식으로만 쓰세요. 당연한 해석은 반복하지 마세요.
- 행동 이름은 반드시 한국어로만 표현하세요. 영어 약어는 절대 쓰지 마세요.
- 한국어로만 쓰세요. 말투는 "했습니다", "유리했습니다"처럼 단정한 존댓말로 쓰세요.
- 인삿말, 이모티콘, 과한 감탄사는 넣지 마세요.
"""


# ── 폴백 피드백 ───────────────────────────────────────────────────────────────

def build_fallback_feedback(summary: dict[str, Any]) -> str:
    h_ko    = action_name_ko(summary["human_action_name"])
    a_ko    = action_name_ko(summary["agent_action_name"])
    h_steps = summary.get("human_reward_steps", [])
    a_steps = summary.get("agent_reward_steps", [])
    h_txt   = ", ".join(f"{s}프레임" for s in h_steps[:5]) if h_steps else "격파가 없었습니다"
    a_txt   = ", ".join(f"{s}프레임" for s in a_steps[:5]) if a_steps else "격파가 없었습니다"
    return (
        f"이 상황에서는 플레이어님이 {h_ko}을 선택했지만, 에이전트의 {a_ko}가 더 유리했습니다. "
        f"에이전트는 현재 관성 방향과 소행성 위치를 함께 고려해 불필요한 추진 없이 발사 타이밍을 잡으며 격파 흐름을 앞당겼습니다.\n\n"
        f"비교 구간에서 플레이어님 쪽 격파는 {h_txt}에, 에이전트 쪽 격파는 {a_txt}에 이어졌습니다. "
        f"다음에 이런 상황이라면, 추진보다 먼저 소행성 방향으로 회전을 맞추고 연속 발사 타이밍을 계획한 뒤 움직여보세요."
    )
